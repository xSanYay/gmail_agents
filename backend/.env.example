# ---- Google OAuth / Gmail ----
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Must exactly match the redirect URI configured in Google Cloud Console.
# Requested callback:
GOOGLE_REDIRECT_URI=https://mako-gorgeous-unicorn.ngrok-free.app/api/callback

# Default: read-only Gmail
GOOGLE_SCOPES=https://www.googleapis.com/auth/gmail.readonly

# ---- App / DB ----
APP_ENV=dev
LOG_LEVEL=INFO
DATABASE_URL=sqlite:///./app.db

# Host header allowlist (TrustedHostMiddleware). Hostnames only (no scheme).
ALLOWED_HOSTS=localhost,127.0.0.1,mako-gorgeous-unicorn.ngrok-free.app

# If you serve the frontend from FastAPI, you can keep this minimal.
CORS_ALLOW_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# Used for signing OAuth state
OAUTH_STATE_SECRET=change-me

# Optional override if you move the frontend folder
# FRONTEND_DIR=frontend
